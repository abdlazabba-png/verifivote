node_modules/
dist/
dev-dist/
.DS_Store
*.local

# Regenerated from node_modules by scripts/copy-ocr-assets.js (postinstall)
public/tesseract/

# Amplify Gen 2: local sandbox state and generated, environment-specific
# outputs (resource ids, endpoints) — created by `npx ampx sandbox`, never
# committed. amplify/backend.ts etc. are the source of truth; these are
# build/deploy artifacts derived from it.
.amplify/
amplify_outputs*.json

# ================= hardened additions =================
# ---- Secrets & credentials (NEVER COMMIT) ----
.env
.env.*
!.env.example
*.local
serviceAccountKey.json
serviceAccount*.json
service-account*.json
*credentials*.json
*.pem
*.p12
*.key
.aws/
aws-exports.js
